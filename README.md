# 📦 ระบบแจ้งเตือนวันหมดอายุผลิตภัณฑ์ (Product Expiry Alert System)

ระบบเว็บแอปพลิเคชันสำหรับจัดการและแจ้งเตือนวันหมดอายุของผลิตภัณฑ์ต่าง ๆ เช่น อาหาร ยา เครื่องสำอาง เป็นต้น พัฒนาด้วย **Node.js (Express)** และ **MySQL (MariaDB)** รองรับระบบสมาชิก, การเพิ่ม/แก้ไข/ลบสินค้า, ระบบ OTP สำหรับรีเซ็ตรหัสผ่าน, ระบบแจ้งเตือนก่อนหมดอายุ และระบบ Admin

---

## 👥 สมาชิกกลุ่ม

* 67021703 ณัฐพล ยศเผ่น       รับผิดชอบ Frontend
* 67022153 รัชชานนท์ หวลคิด    รับผิดชอบ Frontend
* 67022186 วิศรุต กองสี         รับผิดชอบ Frontend เเละ Backend
* 67022300 สิทธินนท์ แสนนาม    รับผิดชอบ Frontend
* 67022344 สิริญากรณ์ การสมมุติ  รับผิดชอบ  Frontend เเละ Mockup

---

## 🛠️ เทคโนโลยีที่ใช้

- **Frontend:** HTML, CSS, JavaScript
- **Backend:** Node.js, Express.js
- **Database:** MySQL / MariaDB
- **Libraries:** mysql2, multer, nodemailer

---

## 📋 ขั้นตอนการติดตั้ง

### ✅ สิ่งที่ต้องมีก่อนเริ่มต้น (Prerequisites)

- [Node.js](https://nodejs.org/) (แนะนำ v18 ขึ้นไป)
- [XAMPP](https://www.apachefriends.org/) หรือ [MariaDB](https://mariadb.org/) (สำหรับฐานข้อมูล)
- [HeidiSQL](https://www.heidisql.com/) หรือ phpMyAdmin (สำหรับจัดการฐานข้อมูล)

---

### 📥 1. ดาวน์โหลดโปรเจกต์

```bash
git clone https://github.com/wisarut442006/project.git
cd project
```

---

### 📦 2. ดาวน์โหลด Library ผ่าน npm

เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:

```bash
npm install
```

คำสั่งนี้จะติดตั้ง Library ทั้งหมดที่จำเป็น ได้แก่:

| Library | เวอร์ชัน | คำอธิบาย |
|---|---|---|
| express | ^5.2.1 | Web framework สำหรับ Node.js |
| mysql2 | ^3.16.1 | ตัวเชื่อมต่อฐานข้อมูล MySQL/MariaDB |
| multer | ^2.0.2 | จัดการอัปโหลดไฟล์ (รูปภาพสินค้า) |
| nodemailer | ^8.0.1 | ส่งอีเมล (สำหรับระบบ OTP) |

---

### 🗄️ 3. Import ฐานข้อมูล

#### 3.1 เปิด XAMPP แล้วเริ่ม  MySQL

#### 3.2 สร้าง User สำหรับฐานข้อมูล

เปิดโปรแกรม **HeidiSQL** แล้วเข้า Session ด้วย:

```
User     : root
Password : (ว่าง)
```

จากนั้นสร้าง User Account ใหม่ โดยไปที่ **Manage user authentication and privileges** แล้วกด **Add**:

```
Username        : expiry
From host       : localhost
Password        : 1234
Repeat Password : 1234
```

> **หมายเหตุ:** ผ่าน **Add object** เลือก database `user` เพื่อให้สิทธิ์เข้าถึงฐานข้อมูล

#### 3.3 Import ไฟล์ SQL

1. เปิดไฟล์ `project.sql` ที่อยู่ในโฟลเดอร์โปรเจกต์
2. ใน HeidiSQL เลือก **File → Load SQL file...** แล้วเลือกไฟล์ `project.sql`
3. กด **Execute (F9)** เพื่อรันคำสั่ง SQL

> ไฟล์ `project.sql` จะสร้างฐานข้อมูล `user` พร้อมตารางและข้อมูลตัวอย่างดังนี้:

| ตาราง | คำอธิบาย |
|---|---|
| `userdata` | ข้อมูลผู้ใช้งาน (email, password, role, status) |
| `categories` | หมวดหมู่สินค้า 7 ประเภท |
| `products` | ข้อมูลสินค้าพร้อมวันผลิต/หมดอายุ |
| `otp` | รหัส OTP สำหรับยืนยันตัวตน |
| `notification_setting` | ตั้งค่าแจ้งเตือนก่อนหมดอายุ |
| `notifications_log` | ประวัติการแจ้งเตือน |

#### 3.4 ทดสอบเชื่อมต่อฐานข้อมูล

เปิด HeidiSQL ด้วย Session ใหม่:

```
User     : expiry
Hostname : localhost
Password : 1234
Database : user
```

ถ้าเชื่อมต่อสำเร็จ แสดงว่าฐานข้อมูลพร้อมใช้งาน ✅

---

### 🚀 4. รันโปรเจกต์

```bash
npm run dev
```

เปิดเบราว์เซอร์แล้วเข้าไปที่:

```
http://localhost:3000
```

---

## 📂 โครงสร้างโปรเจกต์

```
project/
├── home_contact/       # หน้าแรกและหน้าติดต่อ
├── login/              # หน้าเข้าสู่ระบบ / สมัครสมาชิก / รีเซ็ตรหัสผ่าน
├── main/               # หน้าหลัก (แสดงรายการสินค้า / เพิ่มสินค้า / ประวัติ)
├── setting/            # ตั้งค่าผู้ใช้ / การแจ้งเตือน
├── Admin/              # หน้าจัดการผู้ใช้สำหรับ Admin
├── uploads/            # โฟลเดอร์เก็บรูปภาพสินค้า
├── server.js           # Backend API (Express.js)
├── package.json        # รายการ dependencies
├── project.sql         # ไฟล์ SQL สำหรับสร้างฐานข้อมูลพร้อมข้อมูลตัวอย่าง
└── README.md           # ไฟล์นี้
```

---

## 🔑 ข้อมูลบัญชีตัวอย่าง

| บทบาท | Email | Password |
|---|---|---|
| Admin | admin1234@gmail.com | admin1234 |
| User | test123@gmail.com | test123 |
